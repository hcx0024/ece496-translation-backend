# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: If you want to use a different translation API that requires a key
# API_KEY=your_api_key_here

# Optional: Lingva instance base URL (fallback when MyMemory fails). Default: https://lingva.ml
# LINGVA_BASE_URL=https://lingva.ml
